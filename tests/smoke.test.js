const server=require('../server');const assert=require('assert');
(async()=>{await new Promise(r=>server.listen(0,'127.0.0.1',r));let p=server.address().port,base=`http://127.0.0.1:${p}`;
try{let h=await (await fetch(base+'/api/health')).json();assert.equal(h.ok,true);assert.equal(h.version,'anunciae-v1');let home=await (await fetch(base+'/')).text();assert(home.includes('Anunciaê!'));let c=await (await fetch(base+'/api/campaigns',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name:'Teste QA',budget:20})})).json();assert.equal(c.status,'draft');let r=await fetch(base+'/api/integrations/meta',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});assert.equal(r.status,409);let s=await (await fetch(base+'/api/state')).json();assert(s.campaigns.some(x=>x.name==='Teste QA'));console.log('OK: health, frontend, campanha persistida, integração não simulada.');}finally{await fetch(base+'/api/reset',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});server.close()}})().catch(e=>{console.error(e);process.exit(1)});
